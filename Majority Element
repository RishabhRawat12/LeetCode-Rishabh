class Solution {
public:
    int majorityElement(vector<int>& nums) {
        int ele,c1=0,c2=0;
        for(int i=0;i<nums.size();i++){
            if(c1==0){
                ele=nums[i];
                c1=1;
            }
            else if(nums[i]==ele)
                c1++;
            else
                c1--;
        }
        return ele;
    }
};
